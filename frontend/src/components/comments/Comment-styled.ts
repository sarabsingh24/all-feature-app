import styled from 'styled-components';

export const CommentBox = styled.div`
 max-height:240px;
 overflow-y:scroll

`;

export const CommentList = styled.div`
  display: flex;
  font-size: 12px;
  border-radius: 4px;
  padding: 12px;
`;

export const FirstAlpha = styled.div`
  display:flex;
  font-size:12px;
  font-weight:bold;
  text-transform:uppercase;
  border-radius: 50%;
  height:24px;
  width:24px;
  
  margin:4px;
  color:#ffffff;
  justify-content:center;
  align-items:center;
`;

